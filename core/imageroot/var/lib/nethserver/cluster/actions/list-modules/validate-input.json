{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "list-modules input",
    "$id": "http://schema.nethserver.org/cluster/list-modules-input.json",
    "description": "Input is optional. When omitted (null), repositories are queried as usual.",
    "examples": [
        null,
        {"offline": true}
    ],
    "type": ["null", "object"],
    "properties": {
        "offline": {
            "type": "boolean",
            "default": false,
            "description": "If true, no repository is contacted over the network: only installed instances are returned, decorated with locally-synthesized metadata. No update is ever reported in this mode."
        }
    },
    "additionalProperties": false
}
